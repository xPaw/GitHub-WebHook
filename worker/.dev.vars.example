# Copy to .dev.vars for `wrangler dev`. In production set it with `wrangler secret put SECRET`.

# Secret token of the GitHub webhooks, requests that are not signed with it are rejected.
SECRET=hunter2
